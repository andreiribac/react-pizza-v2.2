{
  "compilerOptions": {
    "target": "es5", // Указывает версию ECMAScript, в которую будет транслироваться ваш код
    "lib": [
      "dom",
      "dom.iterable",
      "esnext" // Указывает библиотеки, которые должны быть включены в компиляцию
    ],
    "allowJs": true, // Разрешает компиляцию JavaScript файлов вместе с TypeScript файлами
    "skipLibCheck": true, // Пропускает проверку типов в декларационных файлах (.d.ts)
    "esModuleInterop": true, // Включает возможность импорта CommonJS модулей в стиле ES6
    "allowSyntheticDefaultImports": true, // Разрешает синтетические импорты по умолчанию из модулей, которые их не экспортируют
    "strict": true, // Включает все строгие проверки типов
    "forceConsistentCasingInFileNames": true, // Обеспечивает единообразие в регистре букв в именах файлов
    "module": "esnext", // Указывает кодогенерацию для модулей, поддерживает самые современные стандарты
    "moduleResolution": "node", // Определяет стратегию разрешения модулей
    "isolatedModules": true, // Разрешает модули в изолированном режиме
    "resolveJsonModule": true, // Включает поддержку импорта модулей с расширением .json
    "noEmit": true, // Не генерировать выходные файлы; только проверка типов
    "jsx": "react", // Поддержка JSX в файлах .tsx
    "sourceMap": true, // Генерировать файлы .map для сопоставления исходного кода с компилированным
    "declaration": true, // Генерировать файлы .d.ts для описания типов
    "noUnusedLocals": true, // Сообщать об ошибках на неиспользуемые локальные переменные
    "noUnusedParameters": true, // Сообщать об ошибках на неиспользуемые параметры функций
    "incremental": true, // Включает инкрементальную компиляцию
    "noFallthroughCasesInSwitch": true // Сообщать об ошибках на случаи, когда выполнение переходит в следующий блок switch
  },
  "include": [
    "src/**/*" // Указывает файлы для проверки типов
  ],
  "exclude": [
    "node_modules", // Исключает папку node_modules из проверки
    "build" // Исключает папку build из проверки
  ]
}